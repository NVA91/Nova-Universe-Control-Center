---
# Vollständiges Profil (Full) für maximales Deployment
wiz_profile: "full"
vms_to_create: "{{ deployment_profiles['full'].vms_to_create }}"
apps_to_deploy: "{{ deployment_profiles['full'].apps_to_deploy }}"
